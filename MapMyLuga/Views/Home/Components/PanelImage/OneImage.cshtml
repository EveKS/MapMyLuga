@model MarkerMessageViewModel
<div class="image-container-panel" data-toggle="modal" data-target="#image-modal-one">
    <img src="@Model.FilesPath[0]" class="image-panel-hover" alt="@Model.Title" />
    <div class="image-middle-panel">
        <div class="image-text-panel"><span class="glyphicon glyphicon-zoom-in"></span></div>
    </div>
</div>